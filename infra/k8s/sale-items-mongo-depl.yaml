apiVersion: apps/v1
kind: Deployment
metadata:
  name: sale-items-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sale-items-mongo
  template:
    metadata:
      labels:
        app: sale-items-mongo
    spec:
      containers:
        - name: sale-items-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: sale-items-mongo-srv
spec:
  selector:
    app: sale-items-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
